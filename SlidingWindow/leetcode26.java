public class leetcode26 {
    while fast < nums.length
        slow start at 0
        fast start at 0
        fast move the first num != slow
        assign ++slow with fast
        increment fast until slow != fast

}
